<body}
{{#if user}}
<div class="container">

      <div class="row row-offcanvas row-offcanvas-right">

        <div class="col-xs-12 col-sm-9">
          <p class="pull-right visible-xs">
            <button type="button" class="btn btn-primary btn-xs" data-toggle="offcanvas">Toggle nav</button>
          </p>
          <div class="jumbotron">
            <h1>Welcome , {{user.name}}</h1>
            <p>Edit {{user.name}}'s bio'</p>
          </div>

          {{# each profiles }}
          <div class="row">
          {{# each this }}
            <div class="col-sm-6 col-md-4">
                    <div class="heading">
                        <h2><i class="fa fa-book" aria-hidden="true"></i>{{ this.title }}</h3>
                        <p class="description">{{ this.description }}</p>
                            <a class="btn btn-info" href="/users/profile_add" role="button">View &raquo;</a>
                              <a class="btn btn-success" href="#" role="button">Edit &raquo;</a>
                              <a class="btn btn-warning" href="#" role="button">Delete &raquo;</a>
                                </div>
            </div>
          {{/each}}
          </div>
          {{/each}}
</div>


        <div class="col-xs-6 col-sm-3 sidebar-offcanvas" id="sidebar">
                   <div class="form-group">
                     <input type="text" class="form-control" placeholder="Search">
                  <center>   <button type="submit" class="btn btn-default">Submit</button></center>
                   </div>
<p><p><p><p><p>

          <div class="list-group">
            {{# each profiles }}

            {{# each this }}
            <a href="#" class="list-group-item "><i class="fa fa-book" aria-hidden="true"></i>{{this.title}}</a>
            {{/each}}
            {{/each}}
          </div>
        </div><!--/.sidebar-offcanvas-->
      </div><!--/row-->
    </div>
<p><p><p><p><p><p><p><p><p><p><p>

      {{else}}
     <p><p><p><p><div class="alert alert-danger">Please sign in to view </div><p><p>
      {{/if}}
      </body>
      </div>
