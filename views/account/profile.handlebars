<body}
{{#if user}}
<div class="container">

      <div class="row row-offcanvas row-offcanvas-right">

        <div class="col-xs-12 col-sm-9">
          <p class="pull-right visible-xs">
            <button type="button" class="btn btn-primary btn-xs" data-toggle="offcanvas">Toggle nav</button>
          </p>
          <div class="jumbotron">
            <h1>Welcome , {{user.name}}</h1>
            <p>Edit {{user.name}}'s bio'</p>
          </div>

          {{# each accounts }}
          <div class="row">
          {{# each this }}
            <div class="col-sm-6 col-md-4">
                    <div class="caption">
                    <i class="fa fa-book" aria-hidden="true"></i>
                        <h2>{{ this.title }}</h3>
                        <p class="description">{{ this.description }}</p>
                        <div class="clearfix">

                            <a class="btn btn-default" href="/users/profile_add" role="button">View &raquo;</a>
                              <a class="btn btn-default" href="#" role="button">Edit &raquo;</a>
                              <a class="btn btn-default" href="#" role="button">Delete &raquo;</a>

                    </div>
                </div>
            </div>
          {{/each}}
          </div>
          {{/each}}
</div>
        <div class="col-xs-6 col-sm-3 sidebar-offcanvas" id="sidebar">
                   <div class="form-group">
                     <input type="text" class="form-control" placeholder="Search">
                  <center>   <button type="submit" class="btn btn-default">Submit</button></center>
                   </div>
<p><p>
          <div class="list-group">
            <a href="#" class="list-group-item active">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
            <a href="#" class="list-group-item">Link</a>
          </div>
        </div><!--/.sidebar-offcanvas-->
      </div><!--/row-->


      {{else}}
     <div class="alert alert-danger">Please sign in to view </div>
      {{/if}}
      </body>
      </div>

